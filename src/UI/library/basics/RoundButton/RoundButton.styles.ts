import {StyleSheet} from "react-native";

export default StyleSheet.create({
    screen: {
        overflow: "hidden",
        borderRadius: 35,
    },
    button: {
        borderRadius: 35,
        backgroundColor: '#ccc'
    },
    content: {
        alignItems: "center",
        justifyContent: "center",
    }
})
